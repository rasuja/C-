# C# 入門課題：Challenge 1～15 (完了)
# 🏆 復習テスト 1・2 (完了)

---

# C# 入門課題：Challenge 16「プロの書き方：Razor PagesでHTMLを分離する」

これまでの課題では、C#のコードの中にHTMLを「長い文字列」として書いてきました。
しかし、本番のWeb開発では **「見た目（HTML）」と「ロジック（C#）」は別のファイルに分ける** のが一般的です。

これを実現するASP.NET Coreの強力な機能が **「Razor Pages」** です。

## 課題内容
今のToDoアプリを、Razor Pagesという仕組みを使ってリファクタリング（中身の整理）してみましょう。

1.  **プロジェクト設定の変更**: `Program.cs` で Razor Pages を使うための設定を追加します。
2.  **フォルダの作成**: `Pages` という名前のフォルダを作ります。
3.  **HTMLファイルの作成**: `Pages/Index.cshtml` を作り、そこにHTMLとCSSを移動させます。
4.  **動作確認**: 見た目は変わらなくても、コードが劇的にスッキリしたことを確認してください。

### ヒント
- **Program.cs の変更点**:
```csharp
var builder = WebApplication.CreateBuilder(args);
builder.Services.AddRazorPages(); // Razor Pagesのサービスを追加
var app = builder.Build();

app.MapRazorPages(); // Razor Pagesへの道を作る
app.Run();
```
- **Index.cshtml の中身**:
`@page` というおまじないを一番上に書くことで、そのファイルがWebページになります。

## 進め方
1.  まずは `Pages` フォルダを作るところから始めましょう。
2.  準備ができたら、具体的なファイルの中身を一緒に作っていきましょう！

ここまで学んだこと（C#の基礎、Web API、HTML、CSS、フォーム、CRUD操作）を全て詰め込んだ、自分だけのオリジナルToDoアプリを完成させましょう。

## テスト内容
以下の機能を全て備えたWebアプリを `TodoWeb/Program.cs` に実装してください。

1.  **データの「表示」**:
    - リストに入っているタスクを一覧表示する。
    - デザインを整える（CSSを使ってカード形式にするなど）。
2.  **データの「追加」**:
    - 入力フォームから新しいタスクを追加できる。
3.  **データの「更新（完了）」**:
    - 「完了」ボタンを押すと、そのタスクのマークが変わり、文字が少し薄くなる（任意）。
    - **【追加機能】**: すでに完了しているタスクには「完了」ボタンを表示しないように工夫してください。
4.  **データの「削除」**:
    - 「削除」ボタンを押すと、そのタスクをリストから消す。
5.  **【ボーナス課題】（できれば）**:
    - 合計でいくつのタスクがあるか、画面のどこかに数字で表示してください。

## 進め方
1.  これまでの課題（Challenge 11～15）のコードを振り返りながら、一から書き直してみるつもりで挑戦してください。
2.  ヒントなしでどこまで書けるか試してみましょう！エラーが出たら、メッセージを読んで修正してみてください。

**「完成しました」という報告と、満足のいく画面のスクリーンショットをお待ちしています！**
